[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "triz"
version = "0.1.0"
description = "TRIZ Intelligent System — Agent & Pipeline"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["triz_wrapper"]
